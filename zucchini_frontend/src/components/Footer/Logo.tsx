import styled from "styled-components";

export default function Logo() {
  return <LogoDiv>애호박마켓</LogoDiv>;
}

const LogoDiv = styled.div`
  text-align: center;
  font-size: 2rem;
  font-weight: 600;
  margin-top: 6rem;
  color: lightgray;
`;
